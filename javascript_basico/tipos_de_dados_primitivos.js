const nome = "String";
const nome2 = `template String
               pode pular linha e tals`;
const num1 = 10; //number
const num2 = 10.42; //number
let nomeAluno; //undefined -> não aponta para local nenhum
let nulo = null; // Nulo -> não aponta para local nenhum
const boleano = false; // bool -> true ou false (lógico)

console.log(typeof nome);
console.log(typeof num1);
console.log(typeof nomeAluno);
console.log(typeof nulo);
console.log(typeof boleano);